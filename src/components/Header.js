import React from "react";

const Header = () => {
  return (
    <nav className="navbar navbar-light bg-secondary">
      <div className="container-fluid">
        <span className="navbar-brand mb-0 h1 text-warning">ToDo List</span>
      </div>
    </nav>
  );
};

export default Header;
